import { defineConfig } from '@tarojs/cli'

export default defineConfig({
  projectName: 'treehole-user',
  targets: ['weapp'],
  sourceRoot: 'src',
  outputRoot: 'dist',
  framework: 'react',
  mini: {},
})
